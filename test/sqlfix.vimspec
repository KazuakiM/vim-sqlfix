select * from member;


select a,b, c,  d from   (select *
    from member);

select user_type, count(*) AS count from member group by user_type;


SELECT usr.id AS user_id
 FROM user AS usr
 WHERE usr.now <= :now AND :now < usr.ended_at
 AND usr.status = :active. Bound with now='2015-05-08 15:40:17', :active=1
